#include <iostream>

using namespace std;
//Учебная программа
int main()
{
int a, b;
cout << "Enter A and B: ";
cout << "A + B = " << a + b << '\n';
cout << "A + B = " << a + b << '\n';
cout << "A + B = " << a * b << '\n';
cin >> a >> b;
}